<?xml version="1.0" encoding="utf-8"?>
<!--
  AndroidManifest template injected by the android-port workflow after
  `bunx cap add android` runs. Replaces the auto-generated manifest entirely.

  Differences from the default Capacitor template:
    * <application android:usesCleartextTraffic="false"> so the WebView
      refuses cleartext (required by the Google Play / webview guidelines).
    * <service android:name=".OpencodeService"
              android:foregroundServiceType="dataSync"
              android:exported="false"> registers the Bun host service.
    * INTERNET + FOREGROUND_SERVICE_DATA_SYNC permissions so the service
      can keep the Bun server reachable from the WebView.
    * <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
      is added so the foreground notification can show on Android 13+.
    * android:networkSecurityConfig references res/xml/network_security_config.xml
      (added separately) that pins opencode.local → 127.0.0.1 via a
      cleartextTrafficPermitted=true exception.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:launchMode="singleTask"
            android:theme="@style/AppTheme.NoActionBarLaunch">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".OpencodeService"
            android:foregroundServiceType="dataSync"
            android:exported="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>