<?xml version="1.0" encoding="utf-8"?>
<!--
  Allow cleartext traffic only to the local reverse-proxy host
  (https://opencode.local → http://127.0.0.1:4096). Everywhere else we still
  refuse cleartext, which is what WebView's defaultNetworkSecurityConfig
  requires for production builds.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>